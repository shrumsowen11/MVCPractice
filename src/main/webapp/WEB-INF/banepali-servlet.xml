<?xml version="1.0" encoding="UTF-8"?>


<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="
        http://www.springframework.org/schema/beans     
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/mvc 
        http://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/context 
        http://www.springframework.org/schema/context/spring-context.xsd">


<!-- ********************************************************************************* -->
<!-- This is the .xml file through which the DispatcherWebServlet will take the inputs -->
<!-- DispatcherWebServlet will read this .xml file -->
<!-- ********************************************************************************* -->
 
 
 
    <!-- Enabling all the spring mvc features -->
	<mvc:annotation-driven />
 
   <bean id="dataBaseConnectorUtil" 
      class = "org.springframework.jdbc.datasource.DriverManagerDataSource">
      <property name = "driverClassName" value = "com.mysql.jdbc.Driver"/>
      <property name = "url" value = "jdbc:mysql://localhost:3306/keubiko_db"/>
      <property name = "username" value = "root"/>
      <property name = "password" value = ""/>
   </bean>
   
   <bean id="jdbcTemplateForConnection" class="org.springframework.jdbc.core.JdbcTemplate">
	   <property name="dataSource" ref="dataBaseConnectorUtil"/>
	</bean>
 
	<!-- This is the package name where we keep out model/controller/business logics 
	DispatcherWebServlet asks  which package you will use of the business logics(Works/methods usually called in the Servlet in SpringPractise Project)
	This controller is the MODEL(business layer) -->
	
	<context:component-scan	base-package="com.banepali.controller" />
	<context:component-scan	base-package="com.banepali.dataBase.dao" />
	<!-- After the bean is made, for the package "com.banepali.dataBase.dao", 
	then OBJECTS/BEANS of the classes which are made in that package are created
	and due to which we have the benefits of using the @Autowired -->
	
	
	<mvc:resources mapping = "/Images/**" location = "/Images/" />  
	<!-- ** meaning..anything inside the "Images" folder -->
	
	
	<mvc:resources mapping = "/css/**" location = "/css/" />  
	<!-- ** meaning..anything inside the "Images" folder -->
	
	
	<!-- Hello I am using JSP -->
	<bean id="viewResolver"
		class="org.springframework.web.servlet.view.UrlBasedViewResolver">
		<property name="viewClass"
			value="org.springframework.web.servlet.view.JstlView" />
		<property name="prefix" value="/WEB-INF/pages/" />
		<!-- 	<property name="prefix" value="/" />This takes the called page from the "src/main/webapp",
				but when you give the path like above, you can make the folder  -->
		<property name="suffix" value=".jsp" />
	</bean>


     </beans> 